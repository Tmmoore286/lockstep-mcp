{
  "name": "lunara-mcp-coordinator",
  "version": "0.1.0",
  "description": "Local MCP coordination server for multi-agent workflows",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "lunara-mcp-coordinator": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "node --loader tsx src/cli.ts server",
    "start": "node dist/cli.js server",
    "install:mcp": "node --loader tsx src/cli.ts install",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepare": "npm run build",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  }
}
